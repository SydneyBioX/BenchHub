% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SubmissionDataset.R
\name{buildDatasetTaskSubmission}
\alias{buildDatasetTaskSubmission}
\title{Build DatasetTask submission rows from a Trio object}
\usage{
buildDatasetTaskSubmission(
  trio,
  datasetID = NA_character_,
  taskStage,
  taskType,
  taskName
)
}
\arguments{
\item{trio}{A \code{Trio} object.}

\item{datasetID}{Dataset identifier to link tasks to. Defaults to \code{NA} so the
backend can fill it later.}

\item{taskStage}{A character vector giving the task stage for each task row.}

\item{taskType}{A character vector giving the task type for each task row.}

\item{taskName}{A character vector giving the task name for each task row.}
}
\value{
A \code{data.frame} matching the proposed \code{DatasetTask} table schema.
\code{datasetTaskID} is left as \code{NA}.
}
\description{
This helper creates one \code{DatasetTask} row per supporting evidence name in the
Trio object. It does not upload anything and does not modify the existing
writeCTD() path.
}
\examples{
trio <- BenchHub:::private_example_submission_trio()
buildDatasetTaskSubmission(
  trio,
  taskStage = "prediction",
  taskType = "classification",
  taskName = "class_prediction"
)
}
