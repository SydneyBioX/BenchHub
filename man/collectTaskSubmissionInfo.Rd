% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SubmissionDataset.R
\name{collectTaskSubmissionInfo}
\alias{collectTaskSubmissionInfo}
\title{Collect DatasetTask submission metadata interactively}
\usage{
collectTaskSubmissionInfo(trio, defaults = list(), n_tasks = NULL)
}
\arguments{
\item{trio}{A \code{Trio} object.}

\item{defaults}{Optional named list of pre-filled task values. Supported
entries are \code{taskStage}, \code{taskType}, and \code{taskName}. Each entry may be length 1
or length \code{n_tasks}.}

\item{n_tasks}{Number of tasks to define. If \code{NULL}, interactive sessions
prompt for this value, while non-interactive sessions infer it from the
longest defaults vector.}
}
\value{
A named list ready to pass as \code{task_args} to
\code{buildDatasetTaskSubmission()}.
}
\description{
This helper gathers the task-level fields needed for the \code{DatasetTask}
table. It does not modify the \code{Trio} object.
}
\examples{
data <- data.frame(feature = c(1, 2, 3), row.names = paste0("sample", 1:3))
labels <- factor(c("A", "B", "A"))
names(labels) <- rownames(data)
trio <- Trio$new(
  data = data,
  evidence = list(class_labels = list(
    evidence = labels,
    metrics = "macroF1Metric"
  )),
  metrics = list(macroF1Metric = macroF1Metric),
  name = "example_dataset",
  description = "A small example dataset."
)
collectTaskSubmissionInfo(
  trio,
  defaults = list(
    taskStage = "prediction",
    taskType = "classification",
    taskName = "class_prediction"
  )
)
}
