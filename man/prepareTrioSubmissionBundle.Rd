% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SubmissionDataset.R
\name{prepareTrioSubmissionBundle}
\alias{prepareTrioSubmissionBundle}
\title{Build a pre-submit Trio submission bundle}
\usage{
prepareTrioSubmissionBundle(
  trio,
  dataset_args,
  task_args,
  evidence_task_map,
  prepare_files = FALSE,
  file_args = list(),
  prepare_metrics = FALSE,
  metric_args = list(),
  build_payload = TRUE,
  build_json = FALSE
)
}
\arguments{
\item{trio}{A \code{Trio} object.}

\item{dataset_args}{Named list passed to \code{buildDatasetSubmission()}.}

\item{task_args}{Named list passed to \code{buildDatasetTaskSubmission()}.}

\item{evidence_task_map}{Named character vector mapping Trio evidence names
to submission task names.}

\item{prepare_files}{Logical; if \code{TRUE}, call \code{prepareTrioSubmissionFiles()}.}

\item{file_args}{Named list of additional arguments for
\code{prepareTrioSubmissionFiles()}, excluding \code{trio}.}

\item{prepare_metrics}{Logical; if \code{TRUE}, call
\code{prepareTrioSubmissionMetrics()}.}

\item{metric_args}{Named list of additional arguments for
\code{prepareTrioSubmissionMetrics()}, excluding \code{trio}.}

\item{build_payload}{Logical; if \code{TRUE}, attach the nested payload structure.}

\item{build_json}{Logical; if \code{TRUE}, attach the JSON string.}
}
\value{
A named list containing the built \code{submission}, plus optional
\code{files}, \code{metrics}, \code{payload}, and \code{json}.
}
\description{
This is the higher-level R interface for the new submission workflow. It can
prepare dataset/evidence files, prepare metric metadata, build the five-table
submission object, and return the payload/JSON for inspection before calling
\code{submitTrioSubmission()}.
}
\examples{
data <- data.frame(feature = c(1, 2, 3), row.names = paste0("sample", 1:3))
labels <- factor(c("A", "B", "A"))
names(labels) <- rownames(data)
trio <- Trio$new(
  data = data,
  evidence = list(class_labels = list(
    evidence = labels,
    metrics = "macroF1Metric"
  )),
  metrics = list(macroF1Metric = macroF1Metric),
  name = "example_dataset",
  description = "A small example dataset."
)
dataset_args <- list(
  dataType = "omics",
  dataModality = "transcriptomics",
  technology = "RNA-seq",
  tissue = "blood",
  status = "healthy"
)
task_args <- list(
  taskStage = "prediction",
  taskType = "classification",
  taskName = "class_prediction"
)
bundle <- prepareTrioSubmissionBundle(
  trio = trio,
  dataset_args = dataset_args,
  task_args = task_args,
  evidence_task_map = c(class_labels = "class_prediction")
)
names(bundle)
}
