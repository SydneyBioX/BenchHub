% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SubmissionStudy.R
\name{prepareStudyUpdateSubmission}
\alias{prepareStudyUpdateSubmission}
\title{Prepare an update submission from an existing Study version}
\usage{
prepareStudyUpdateSubmission(
  studyID,
  study = NULL,
  datasetIDs = NULL,
  description = NULL,
  protocolGist = NULL,
  mappingFunctions = NULL,
  studies = NULL,
  study_datasets = NULL,
  ss = "1H8hOxL8D0XTquao8vGZ2cr9-XeaFC48SWAdFn0M3fkg",
  build_payload = TRUE,
  build_json = TRUE,
  review = TRUE,
  submit = FALSE,
  url = submission_webapp_url,
  submittedBy = NULL
)
}
\arguments{
\item{studyID}{Existing Study identifier to use as the update baseline.}

\item{study}{Optional \code{BenchmarkStudy} object. When omitted, a new one is
created from the existing Study row.}

\item{datasetIDs}{Optional replacement dataset IDs. Defaults to the linked
datasets from the baseline Study.}

\item{description}{Optional replacement description.}

\item{protocolGist}{Optional replacement protocol gist URL.}

\item{mappingFunctions}{Optional replacement mapping functions gist URL.}

\item{studies}{Optional data frame of Study rows.}

\item{study_datasets}{Optional data frame of StudyDataset rows.}

\item{ss}{Submission spreadsheet ID or URL.}

\item{build_payload}{Whether to include the nested payload. Defaults to
\code{TRUE}.}

\item{build_json}{Whether to include JSON output. Defaults to \code{TRUE}.}

\item{review}{Whether to print a short submission summary. Defaults to
\code{TRUE}.}

\item{submit}{Whether to submit immediately. Defaults to \code{FALSE}.}

\item{url}{Google Apps Script endpoint URL. Defaults to the package
submission web app.}

\item{submittedBy}{Submitter email or identifier.}
}
\value{
A named list containing the updated Study submission bundle.
}
\description{
Prepare an update submission from an existing Study version
}
\examples{
studies <- data.frame(
  studyID = "study_001",
  studyName = "example_study",
  version = "0.0.1",
  description = "A small example benchmark study.",
  type = "original",
  protocolGist = "",
  mappingFunctions = "",
  stringsAsFactors = FALSE
)
study_datasets <- data.frame(
  studyDatasetID = "study_dataset_001",
  studyID = "study_001",
  datasetID = "dataset_001",
  stringsAsFactors = FALSE
)
result <- prepareStudyUpdateSubmission(
  studyID = "study_001",
  studies = studies,
  study_datasets = study_datasets,
  build_json = FALSE,
  review = FALSE
)
names(result)
}
