% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SubmissionDataset.R
\name{writeSubmission}
\alias{writeSubmission}
\title{Interactively prepare a Trio submission bundle}
\usage{
writeSubmission(
  trio,
  n_tasks = NULL,
  dataset_defaults = list(),
  task_defaults = list(),
  evidence_defaults = list(),
  metric_defaults = list(),
  prepare_files = TRUE,
  file_args = list(),
  upload_custom_metrics = FALSE,
  githubPat = Sys.getenv("GITHUB_PAT"),
  gistPublic = TRUE,
  review = TRUE,
  submit = NULL,
  url = submission_webapp_url,
  submittedBy = NULL,
  build_payload = TRUE,
  build_json = FALSE
)
}
\arguments{
\item{trio}{A \code{Trio} object.}

\item{n_tasks}{Optional number of tasks to define. If \code{NULL}, interactive
sessions prompt for it and non-interactive sessions require it through
\code{task_defaults} or \code{n_tasks}.}

\item{dataset_defaults}{Optional named list passed into
\code{collectDatasetSubmissionInfo()}.}

\item{task_defaults}{Optional named list passed into
\code{collectTaskSubmissionInfo()}.}

\item{evidence_defaults}{Optional named list passed into
\code{collectEvidenceSubmissionInfo()}.}

\item{metric_defaults}{Optional named list passed into
\code{collectMetricSubmissionInfo()}.}

\item{prepare_files}{Logical; if \code{TRUE}, prepare dataset/evidence files or
reuse an existing source.}

\item{file_args}{Optional named list passed into
\code{prepareTrioSubmissionFiles()}.}

\item{upload_custom_metrics}{Logical; if \code{TRUE}, upload custom metrics to a
GitHub gist.}

\item{githubPat}{Optional GitHub personal access token. Defaults to the
current \code{GITHUB_PAT} environment variable.}

\item{gistPublic}{Logical; whether any created gist should be public.}

\item{review}{Logical; if \code{TRUE}, print a compact review summary before
returning.}

\item{submit}{Logical; if \code{TRUE}, submit the built payload at the end. If
\code{NULL}, interactive sessions ask whether to submit after the review step.}

\item{url}{Optional Google Apps Script endpoint URL used when
\code{submit = TRUE}.}

\item{submittedBy}{Optional submitter email or identifier used when
\code{submit = TRUE}.}

\item{build_payload}{Logical; if \code{TRUE}, attach the nested payload object.}

\item{build_json}{Logical; if \code{TRUE}, attach the JSON string.}
}
\value{
A named list containing collected arguments, optional prepared file
and metric metadata, the final \code{submission}, and optional \code{payload}/\code{json}.
}
\description{
This high-level helper provides a \code{writeCTD()}-style console workflow for
the new five-table submission path. It collects dataset, task, evidence, and
metric metadata; optionally prepares local files for upload and verifies a
Figshare article; optionally uploads custom metrics to a GitHub gist; and
returns all submission tables ready for review before calling
\code{submitTrioSubmission()}.
}
\examples{
trio <- BenchHub:::private_example_submission_trio()
result <- writeSubmission(
  trio = trio,
  n_tasks = 1,
  dataset_defaults = BenchHub:::private_example_dataset_args(),
  task_defaults = BenchHub:::private_example_task_args(),
  evidence_defaults = list(
    taskName = "class_prediction",
    evidenceType = "experimental_ground_truth"
  ),
  metric_defaults = list(metricType = "label_based"),
  prepare_files = FALSE,
  build_json = FALSE,
  review = FALSE
)
names(result)
}
