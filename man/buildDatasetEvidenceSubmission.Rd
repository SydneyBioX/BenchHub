% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SubmissionDataset.R
\name{buildDatasetEvidenceSubmission}
\alias{buildDatasetEvidenceSubmission}
\title{Build DatasetEvidence submission rows from a Trio object}
\usage{
buildDatasetEvidenceSubmission(
  trio,
  datasetTaskID,
  evidenceName = names(trio$evidence),
  evidenceType = NA_character_
)
}
\arguments{
\item{trio}{A \code{Trio} object.}

\item{datasetTaskID}{Task identifier to link evidence rows to. Defaults to
generated task IDs when not supplied.}

\item{evidenceName}{Character vector of Trio evidence names to include.
Defaults to all evidence in \code{trio}.}

\item{evidenceType}{Either \code{"columns"} or \code{"figshare"} indicating
where to find the evidence.}
}
\value{
A \code{data.frame} matching the proposed \code{DatasetEvidence} table schema.
\code{evidenceID} is left as \code{NA}.
}
\description{
This helper creates one \code{DatasetEvidence} row per supporting evidence entry
in the Trio object. It does not upload anything and does not modify the
existing writeCTD() path.
}
\examples{
data <- data.frame(feature = c(1, 2, 3), row.names = paste0("sample", 1:3))
labels <- factor(c("A", "B", "A"))
names(labels) <- rownames(data)
trio <- Trio$new(
  data = data,
  evidence = list(class_labels = list(
    evidence = labels,
    metrics = "macroF1Metric"
  )),
  metrics = list(macroF1Metric = macroF1Metric),
  name = "example_dataset",
  description = "A small example dataset."
)
buildDatasetEvidenceSubmission(
  trio,
  datasetTaskID = "task_001",
  evidenceType = "experimental_ground_truth"
)
}
