% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SubmissionDataset.R
\name{buildDatasetSubmission}
\alias{buildDatasetSubmission}
\title{Build a Dataset submission row from a Trio object}
\usage{
buildDatasetSubmission(
  trio,
  name = private_submission_dataset_name(trio),
  dataType,
  dataModality,
  technology,
  description = trio$description,
  doi = NA_character_,
  organism = NA_character_,
  tissue,
  status
)
}
\arguments{
\item{trio}{A \code{Trio} object.}

\item{name}{Dataset name for submission. Defaults to the existing Trio name,
falling back to \code{trio$dataSourceID} when needed.}

\item{dataType}{One of \code{"omics"} or \code{"clinical"}.}

\item{dataModality}{One of \code{"genomics"}, \code{"transcriptomics"},
\code{"epigenomics"}, \code{"proteomics"}, \code{"metabolomics"}, \code{"patient"},
or \code{"other"}.}

\item{technology}{Free-text technology description.}

\item{description}{Dataset description. Defaults to \code{trio$description} if
present.}

\item{doi}{DOI reference for the dataset.}

\item{organism}{Free-text organism label.}

\item{tissue}{Free-text tissue label.}

\item{status}{One of \code{"healthy"}, \code{"diseased"}, \code{"developmental"},
or \code{"other"}.}
}
\value{
A one-row \code{data.frame} matching the proposed \code{Dataset} table schema.
\code{datasetID} is left as \code{NA} because it is expected to be generated by the
submission backend.
}
\description{
This helper is the first step in the new submission workflow. It does not
upload anything and does not modify the existing writeCTD() path.
}
\examples{
data <- data.frame(feature = c(1, 2, 3), row.names = paste0("sample", 1:3))
labels <- factor(c("A", "B", "A"))
names(labels) <- rownames(data)
trio <- Trio$new(
  data = data,
  evidence = list(class_labels = list(
    evidence = labels,
    metrics = "macroF1Metric"
  )),
  metrics = list(macroF1Metric = macroF1Metric),
  name = "example_dataset",
  description = "A small example dataset."
)
buildDatasetSubmission(
  trio,
  dataType = "omics",
  dataModality = "transcriptomics",
  technology = "RNA-seq",
  tissue = "blood",
  status = "healthy"
)
}
