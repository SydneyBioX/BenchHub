% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SubmissionDataset.R
\name{prepareTrioSubmissionFiles}
\alias{prepareTrioSubmissionFiles}
\title{Prepare Trio files and download metadata for submission}
\usage{
prepareTrioSubmissionFiles(
  trio,
  name = trio$name,
  outputDir = ".",
  saveData = NULL,
  saveEvidence = NULL,
  useExistingSource = NULL,
  figshareUrl = NULL,
  datasetFileName = NULL,
  evidenceFileName = NULL,
  verifyFigshare = FALSE,
  skipMd5Check = FALSE
)
}
\arguments{
\item{trio}{A \code{Trio} object.}

\item{name}{Dataset name used for generated filenames. Defaults to
\code{trio$name}.}

\item{outputDir}{Directory for generated \code{.rds} files. Defaults to \code{"."}.}

\item{saveData}{Logical indicating whether to save \code{trio$data} to an RDS
file. If \code{NULL} and interactive, the user is prompted.}

\item{saveEvidence}{Logical indicating whether to save all supporting
evidence to a single RDS file. If \code{NULL} and interactive, defaults to
\code{TRUE}.}

\item{useExistingSource}{Logical indicating whether to keep using
\code{trio$dataSource}/\code{trio$dataSourceID} rather than preparing uploaded files.
If \code{NULL} and interactive, the user is prompted when an existing source is
available.}

\item{figshareUrl}{Optional Figshare URL to verify against prepared files.}

\item{datasetFileName}{Optional dataset filename to match inside the Figshare
article.}

\item{evidenceFileName}{Optional evidence filename to match inside the
Figshare article.}

\item{verifyFigshare}{Logical; if \code{TRUE}, verify the prepared files against
the Figshare article metadata.}

\item{skipMd5Check}{Logical; if \code{TRUE}, allow verification without MD5
matching.}
}
\value{
A named list describing the saved files and/or reusable download
sources for dataset and evidence.
}
\description{
This helper reuses the good parts of the old \code{writeCTD()} workflow for the
new submission path. It can either save dataset/evidence files locally so
the user can upload them, or reuse an existing downloadable source already
attached to the \code{Trio}.
}
\examples{
data <- data.frame(feature = c(1, 2, 3), row.names = paste0("sample", 1:3))
labels <- factor(c("A", "B", "A"))
names(labels) <- rownames(data)
trio <- Trio$new(
  data = data,
  evidence = list(class_labels = list(
    evidence = labels,
    metrics = "macroF1Metric"
  )),
  metrics = list(macroF1Metric = macroF1Metric),
  name = "example_dataset",
  description = "A small example dataset."
)
files <- prepareTrioSubmissionFiles(
  trio,
  outputDir = tempdir(),
  saveData = TRUE,
  saveEvidence = TRUE,
  useExistingSource = FALSE
)
names(files)
}
